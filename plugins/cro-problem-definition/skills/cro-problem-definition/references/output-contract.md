# 사용자 문제정의 — 출력 계약 (Output Contract) v2.1

> **v2.2 변경 (2026-08-11, DEC-003)**
> BIAS 판정(T2)을 15항목 → **27항목**(B9/I7/A7/S4)으로 확대. 기존 항목ID 전부 유지, `B5`~`B9`·`I5`~`I7`·`A5`~`A7`·`S4` 추가 채번, B4 이름 "인지 부하 최소화" → "선택지 과부하 방지". 27항목 정본 표는 SSOT `50_reference/bias-checklist.md`.


> **이 파일은 계약 v2.1의 사본**입니다(스킬 배포용). 원본은 코치 리포지터리의
> `prep-week/_specs/pd-output-contract.md`이며, 원본 개정 시 이 사본을 동기화합니다.
> 스킬은 아래의 **열 이름·순서·ID 체계·공식을 문자 그대로** 따릅니다.

> **v2.1 변경 (2026-08-10, 우디 지시 — 시트 단순화)**
> 1. **시트에 남는 것은 T4 문제정의 리스트 하나뿐.** T1·T2·T3는 스킬이 대화에서 만드는 **작업 산출물**이며 시트에 옮기지 않는다.
> 2. **T4에 `화면`·`BIAS 항목`·`CRO 항목` 열 추가**, `근거 ID` 열 삭제 → 12열에서 **14열**로. 문제 하나를 보면 "어느 화면에서 / 무슨 문제이고 / BIAS 어느 항목 / CRO 어느 항목에 걸리는지"가 한 줄로 읽힌다.
> 3. **BIAS 27항목·CRO 체크리스트는 판정란 없이 `참고_BIAS_CRO_체크리스트` 탭으로 분리** — 사람이 판정할 때 문구를 찾아보는 용도.
> 4. Impact 점수 열과 근거 필수 규칙은 **그대로 유지**(금요일 ICE의 I 근거값).

> **v2.0 변경 (설계 전환)**
> 1. **TSV 자동 붙여넣기 체계 폐기** — 셀 주소(A11/A34/…), 시트 수식(N·O열), 드롭다운, 137행 행 맵을 전부 버립니다. 스킬은 **대화 안에 마크다운 표**를 출력하고, 담당자가 그 표를 복사해 시트 템플릿에 옮겨 적습니다.
> 2. **문제 문장 형식 교체** — 1인칭("나는 ~") → **고객 주어**("고객은 이 [화면/상황]에서 ~").
> 3. **Impact를 스킬이 계산** — 가중치 적용과 최종 Impact를 스킬이 계산해 과정과 함께 제시하고, 담당자가 피드백하면 재계산합니다(시트 수식 아님).
> 4. **정성 근거(VOC·인터뷰)를 정식 인풋으로** 추가.
> 5. 메타카드의 `마스킹 완료` 체크 항목 삭제 — PII 감지·중단은 스킬의 상시 행동 규칙으로만 유지.

> **지위**: 이 파일이 **단일 정본(SSOT)**입니다. `03f`(시트 템플릿)·`03d`(CRO 발췌본)·`cro-problem-definition` 스킬은 전부 이 계약의 표 이름·열 순서·ID 체계·공식을 그대로 복사합니다. 이 파일을 수정하면 세 산출물을 반드시 동기화합니다.
> **용도**: 8/10(월) 문제영역 선정 워크숍 직후 진행하는 **사용자 문제정의 라이브 세션(60~90분)**의 산출물 규격.
>
> **범위 주의**: 합성 인터뷰(03b)는 Sprint 1~2로 이연 — 이 계약에 합성 인터뷰 섹션은 없습니다.

---

## 1. 산출물 구조 — 표 4종

라이브 세션에서 스킬이 대화에 출력하는 표는 아래 4종입니다. 담당자는 이 표를 복사해 시트 템플릿(`03f`)의 같은 이름 섹션에 붙입니다.

| 표 | 이름 | 언제 | 시트 이식 |
|---|---|---|---|
| **T1** | 워크스루 Fact 로그 | STEP 1 (전사 확인 후) | ✗ 대화에만 (진단 근거) |
| **T2** | BIAS 27항목 판정 | STEP 2 | ✗ 대화에만 (판정 과정) |
| **T3** | CRO 체크리스트 판정 | STEP 3 (B축 생략 가능) | ✗ 대화에만 (판정 과정) |
| **T4** | **문제정의 리스트** | STEP 5 | **✅ 유일한 시트 산출물** |

**T1~T3는 시트에 옮기지 않습니다.** 진단 과정은 대화에 남고, 그 결과로 나온 문제만 T4로 시트에 착지합니다. T4의 `BIAS 항목`·`CRO 항목` 열이 "이 문제가 어느 체크에 걸렸는가"를 담으므로 판정 표 자체는 시트에 없어도 추적이 됩니다.

**시트 탭 구성** (현행 워크숍 시트):

| 탭 | 내용 |
|---|---|
| `A축_문제정의` / `B축_문제정의` | 기본 정보 몇 줄 + **T4 문제정의 리스트** |
| `A축_문제정의_예시` | 채워진 작성 예시 |
| `참고_BIAS_CRO_체크리스트` | BIAS 27항목 + 축별 CRO 항목 문구 (**판정란 없음 — 읽기용**) |

상세는 `03f`.

## 2. 표별 열 정의 (이름·순서 고정)

**표의 열 이름과 순서는 계약·시트 템플릿·스킬 출력 예시에서 문자 단위로 동일해야 합니다.** 이것이 이 계약의 유일한 정합 조건입니다.

### T1. 워크스루 Fact 로그 — 6열

| # | 열 이름 | 내용 |
|---|---|---|
| 1 | Fact ID | `F-01`, `F-02`, … (화면 1개 = 1회 실행 단위로 연번) |
| 2 | 스크린샷 | 파일명 또는 "1번/2번/3번" |
| 3 | 위치·요소 | 예: 상단 헤드라인, 하단 CTA |
| 4 | 관측 사실 | 문구 전사 또는 "○○ 요소 없음" — **없음도 Fact** |
| 5 | 요소 상태 | `있음` / `없음` / `판독불확실❓` |
| 6 | 안 함 vs 못 함 | `클릭하지 않았다` / `클릭할 수 없었다` / `-` |

### T2. BIAS 27항목 판정 — 6열 (27행 고정)

| # | 열 이름 | 내용 |
|---|---|---|
| 1 | 항목ID | `B1`~`B4`, `I1`~`I4`, `A1`~`A4`, `S1`~`S3` (03a §6 순서 고정) |
| 2 | 단계 | Block / Interpret / Act / Store |
| 3 | 판정 | `O`(Pass) / `△`(부분·판단 보류) / `X`(Fail) |
| 4 | 근거 Fact ID | T1 인용, 콤마 구분 — **X 판정은 필수** |
| 5 | 근거 요약 | 관측 사실 한 줄 |
| 6 | 문제 문장 초안 | X·△만. 고객 주어 형식(4절) |

체크 항목 문구는 시트 템플릿에 미리 적혀 있으므로 표에 넣지 않습니다(항목ID로 대응).

### T3. CRO 체크리스트 판정 — 5열

| # | 열 이름 | 내용 |
|---|---|---|
| 1 | 항목ID | `LP-S#`·`LP-C#`·`PP-C#`·`PP-P#`(A축) / `CO-F#`·`CA-T#`(B축) — 5절 |
| 2 | 체크 항목 | `03d`·스킬 data JSON의 `item_ko` 요지 |
| 3 | 판정 | `Pass` / `Fail` / `N/A` |
| 4 | 근거 Fact ID | Fail은 필수 |
| 5 | BIAS 단계 | 03d 매핑 그대로 |

표 아래 본문에 2줄을 함께 냅니다: **지배 병목 단계** 1문장(동률이면 Block > Interpret > Act > Store 우선), **Fail율** = Fail ÷ (Fail + Pass), N/A는 분모 제외.

### T4. 문제정의 리스트 — 14열 ★ 유일한 시트 산출물

| # | 열 이름 | 내용 |
|---|---|---|
| 1 | 문제ID | `PR-01`, … |
| 2 | 화면 | 이 문제가 관측된 화면명 (메타카드의 화면명) |
| 3 | 고객 문제 | 고객 주어 문장(4절) |
| 4 | BIAS 단계 | Block / Interpret / Act / Store |
| 5 | BIAS 항목 | **항목ID + 체크 항목 요약** — 예: `B1 1초 직관성`. 해당 없으면 `-`, 복수면 콤마 |
| 6 | CRO 항목 | **항목ID + 체크 항목 요약** — 예: `LP-S5 메인 CTA가 가장 눈에 띈다`. 해당 없으면 `-`, 복수면 콤마 |
| 7 | 근거유형 | `관측` / `정성(VOC·인터뷰)` / `관측+정성` |
| 8 | 트래픽 점수 | 1~10 (참가자가 준 트래픽 값 기반) |
| 9 | 트래픽 근거 | 한 줄 — **오전 워크숍 시트에서 확인한 트래픽 값 인용** |
| 10 | Gap 점수 | 1~10 (참가자가 준 Gap 배수 기반) |
| 11 | Gap 근거 | 한 줄 — **벤치마크 Gap 배수 인용** |
| 12 | 심각도(가중) | 원점수에 BIAS 가중치를 적용한 값(6절) |
| 13 | 심각도 근거 | 한 줄 — 원점수·Fail율·가중치 적용 내역 |
| 14 | **Impact** | 스킬 계산값, 소수 1자리 |

`BIAS 항목`·`CRO 항목`이 이 문제의 **근거 기록**입니다(v2.0의 `근거 ID` 열을 대체). Fact ID는 대화의 T1에만 남고 시트에는 옮기지 않습니다.

Impact 내림차순으로 정렬합니다. 점수 3개 중 하나라도 못 구하면 그 칸과 Impact를 공란으로 두고 근거 칸에 `코치 확인 필요`라고 씁니다.

## 3. 표 출력 규칙 (스킬·프롬프트 카드 공통)

1. 표는 **마크다운 표**로 대화에 출력합니다. 코드블록에 넣지 않습니다 — 렌더된 표라야 복사해서 시트에 붙일 때 셀로 갈라집니다.
2. 표 바로 위 또는 아래에 **어느 시트 섹션에 붙이는지** 한 줄로 안내합니다. 예: "이 표를 시트 `A축_문제정의` 탭의 **BIAS 27항목 판정** 섹션에 붙여 넣으세요."
3. 열 이름·순서는 2절과 문자 단위로 동일해야 합니다. 임의로 열을 추가·삭제·재배열하지 않습니다.
4. 셀 안에서 줄바꿈하지 않습니다(시트 붙여넣기가 깨집니다). 콤마·슬래시로 구분합니다.
5. **표가 시트에 한 셀로 붙는 경우**: 렌더된 표를 드래그해 복사했는지 확인하고, 그래도 안 되면 열별로 나눠 옮깁니다.

## 4. 고객 문제 문장 형식 (정본)

```
고객은 이 [화면/상황]에서 이런 [문제]를 [못 한다 / 모른다 / 느낀다]
```

- 주어는 **사람(고객)** 입니다. 화면·버튼이 주어면 그건 Fact지 문제가 아닙니다.
- **솔루션 역기술 금지**: 문장에서 해결책이 자동으로 따라 나오면 아직 현상입니다. 판별 질문 — *"이 문장에서 솔루션이 자동으로 나오는가?"* 나오면 *"그래서 고객이 무엇을 못 하고 있나?"*로 한 번 더 내려가 다시 씁니다.
- **근거 없는 추측 금지**: 확정 전사(T1)나 정성 근거에 없는 감정·수치·발화를 지어내지 않습니다. 모든 문장은 근거 ID를 답니다.

예시:
- 현상 ❌ "진행 단계 표시가 없다" (→ "표시를 넣자"가 바로 나옴)
- 문제 ✅ "고객은 이 정보 입력 대화에서 앞으로 얼마나 더 남았는지 몰라, 끝이 안 보인다고 느껴 중간에 답장을 멈춘다"

## 5. ID 체계

| ID | 형식 | 부여 주체 |
|---|---|---|
| Fact ID | `F-01`~ (실행 단위 연번) | 스킬 STEP 1 |
| BIAS 항목ID | `B1`~`S4` 27항목 (SSOT `50_reference/bias-checklist.md` 고정) | DEC-003 |
| CRO 항목ID | 아래 표 | 03d 제작 시 부여 |
| 문제ID | `PR-01`~ | 스킬 STEP 4 |
| 트리 노드ID | `N1-1-1` 형식 | 과제(03e) 수행 시 |

| 접두사 | 섹션 | 축 |
|---|---|---|
| `LP-S#` | Landing Page — Structure & CTA | A |
| `LP-C#` | Landing Page — Content & Urgency | A |
| `PP-C#` | Product Page — CTA Area | A |
| `PP-P#` | Product Page — Social Proof | A |
| `CO-F#` | Checkout Page — Form & Process | B |
| `CA-T#` | Cart Page — Trust & Clarity | B |

- 번호는 원본 `E-commerce CRO Checklist.MD`(175항목)의 섹션 내 등장 순서.
- 각 항목은 `{id, section, item_ko, bias_stage, screen_hint(A축) | kakao_reinterpretation(B축), pass_criteria, fail_signal}` 필드를 가집니다. `03d` MD와 스킬 JSON의 항목 수·ID·`item_ko`·힌트 문구는 **문자 단위로 동일**해야 합니다(`pass_criteria`·`fail_signal`은 JSON 전용 보조 필드).

## 6. Impact 공식 · BIAS 가중치 (정본 — 03a §5 원문)

```
문제 Impact 점수 = (트래픽 영향 + 벤치마크 Gap + 정성 심각도) / 3   (각 10점 만점)
```

- 정성 심각도 가중치: **Block 실패 항목이 있으면 +2, Interpret 실패 항목이 있으면 +1**을 원점수에 더한 뒤 **10점으로 캡**. Act/Store 실패는 가중치 없음.
- **계산 주체**: 스킬이 계산합니다. 가중치 적용과 평균을 **계산 과정과 함께** 보여주고(원점수 → 가중 → Impact), 담당자가 점수나 근거에 이견을 주면 **반영해 재계산**합니다.
- **입력값은 지어내지 않습니다**: 트래픽 값과 벤치마크 Gap 배수는 오전 워크숍 시트의 해당 축 탭에서 확인한 값을 참가자에게 물어서만 받습니다. 못 받으면 공란 + `코치 확인 필요`.
- 각 점수에 반드시 근거 한 줄 — **근거 없는 점수는 무효**입니다.
- **ICE는 이 단계에서 계산하지 않습니다** (8/14 금요일 워크숍, **합산식 ICE = I+C+E**가 정본 — 곱셈식 금지). 이 세션의 Impact는 금요일 ICE의 **I 근거값**일 뿐입니다.

## 7. 소비자별 사용 지점

| 산출물 | 이 계약에서 가져가는 것 |
|---|---|
| `03f` 시트 템플릿 | 1·2절 표 구조와 열 이름, 4절 문장 형식, 완료 체크 |
| `03d` + 스킬 data JSON | 5절 ID 체계·항목 필드 스키마 |
| 스킬 `cro-problem-definition` | 2절 열 정의, 3절 표 출력 규칙, 4절 문장 형식, 5절 ID 부여, 6절 공식·계산 주체 |
| `03a`·`03g`·`03e` | 전체 참조 |

## 8. 완료 판정 체크 (7항목)

1. 스크린샷 전사가 "전사 확인"으로 승인되었는가
2. BIAS 27항목 전부 판정되었는가 (빈 판정 없음)
3. X·△ 항목마다 문제 문장 초안이 있는가
4. 문제 문장에서 솔루션이 자동으로 따라 나오지 않는가 (현상 아님)
5. 문제 문장이 전부 **고객 주어** 형식인가
6. 모든 점수(트래픽·Gap·심각도)에 근거 한 줄이 붙어 있는가
7. T4 문제정의 리스트가 시트에 옮겨졌는가
